//
// Created by 觉而不绝 on 2023/3/18.
//

#include "comm.h"

using namespace std;

class Solution
{
public:
    bool checkPalindromeFormation(string a, string b)
    {
        if(a.length() == 0 || a.length() == 1)
            return true;
        return false;
    }
};